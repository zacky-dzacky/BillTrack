<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white" 
    android:padding="16dp"
    tools:context=".LoginActivity">

    <!-- Wallet Icon with Circular Background -->
    <ImageView
        android:id="@+id/loginWalletIcon"
        android:layout_width="100dp" 
        android:layout_height="100dp"
        android:src="@drawable/ic_wallet_login"
        android:background="@drawable/login_icon_circle_background"
        android:padding="24dp" 
        android:scaleType="fitCenter"
        android:contentDescription="Wallet Icon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/loginTitleTextView"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginTop="64dp"/> 

    <!-- Welcome Title -->
    <TextView
        android:id="@+id/loginTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login_welcome_title"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/login_text_main_color"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@+id/loginWalletIcon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/loginSubtitleTextView"/>

    <!-- Subtitle -->
    <TextView
        android:id="@+id/loginSubtitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login_subtitle"
        android:textSize="16sp"
        android:textColor="@color/login_text_secondary_color"
        android:layout_marginTop="8dp"
        android:gravity="center_horizontal"
        app:layout_constraintTop_toBottomOf="@+id/loginTitleTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/googleSignInButton"/>

    <com.google.android.gms.common.SignInButton
        android:id="@+id/googleSignInButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/login_google_button_text"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/login_button_background_teal"
        app:icon="@drawable/ic_google_g_logo"
        app:iconTint="@android:color/white"
        app:iconGravity="textStart"
        app:iconPadding="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:layout_marginBottom="32dp"
        app:cornerRadius="24dp"
        app:layout_constraintBottom_toTopOf="@+id/loginFooterTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Footer Text -->
    <TextView
        android:id="@+id/loginFooterTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/login_footer_agreement"
        android:textSize="12sp"
        android:textColor="@color/login_text_secondary_color"
        android:gravity="center_horizontal"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/loginProgressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/googleSignInButton"
        app:layout_constraintBottom_toBottomOf="@id/googleSignInButton"
        app:layout_constraintStart_toStartOf="@id/googleSignInButton"
        app:layout_constraintEnd_toEndOf="@id/googleSignInButton"
        tools:visibility="gone"/>

</androidx.constraintlayout.widget.ConstraintLayout>
